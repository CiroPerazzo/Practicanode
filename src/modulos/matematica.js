const suma = (a, b)=>a + b


const resta = (a, b) => a - b

function multiplicarPorPI(a)
{
    const pi = 3.14

    let resultado

    resultado = a * pi

    return resultado
}


function dividir(a, b)
{
    return a/ b
}

export {suma, resta, multiplicarPorPI, dividir}